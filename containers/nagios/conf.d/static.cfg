
define host {
  use                     linux-server
  host_name               headnode
  address                 10.0.0.1
  hostgroups              server
}

define host {
  use                     linux-server
  host_name               tellico
  address                 160.36.131.202
  check_command           check_ssh
}

define host {
  use                     linux-server
  host_name               icl.utk.edu
  address                 160.36.239.231
  check_command           check_ssh
}

define service {
  check_command         check_https!icl.utk.edu
  host			icl.utk.edu
  use                   icl-service
  service_description   web server
  max_check_attempts    1    ; Re-check the service up to N times in order to determine its final (hard) state
  check_interval        5    ; Check the service every N minutes under normal conditions
  retry_interval        1    ; Re-check the service every N minutes until a hard state can be determined
}

define host {
  use                     linux-server
  host_name               synology.icl.utk.edu
  address                 160.36.131.195
}

define hostgroup {
  hostgroup_name          server
}

