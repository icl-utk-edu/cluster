- Create a class for "nodes" which has structure for querying clsuter config
- use full dns names for headnode.cluster and nodes
- clock sync
- use ansible to configure node image
   - use external docker context to get ansible config
