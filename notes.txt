Need DNS to point to local dnsmasq daemon

Provisioning Strategy:
  - Boot node into default OS image
  - SSH to node to get BMC IP
  - Insert node into database
  - Use local ipmitool to set IPMI username and password


